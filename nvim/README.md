# Neovim

I use this config for Go development

If you want to use the config download files and copy to .config/nvim then install packer. Use :PackerSync in neovim to download and install all plugins. Then restart the neovim and you can use.
